<h1>Books</h1>

<% @books.each do |book| %>
 <p>Title</p>
 <span><%= book.title %></span>
 
 <p>Body</p>
 <span><%= book.body %></span>

<% end %>

 <%= link_to "Show",book.title, show_book_path(@book.id) %>
 <%= link_to "Edit",book.edit, edit_book_path(@book.id) %>